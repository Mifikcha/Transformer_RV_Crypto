_get_data.py - общий скрипт получения данных. Включает в себя все остальные скрипты
get_OHLCV.py - нужен для подтягивания данных с биржи. Примеры использования внутри файла
validate_OHLCV.py - простая предобработка данных, полученных get_OHLCV. Пример использования внутри файла
resample_OHLCV.py - ресемплинг данных (из 1 минуты получаются 5 минут, 15 минут, 30 минут, 1 час, 4 часа)
sanity_check_OHLCV.py - проверка на норм / не норм данных
compare_AGR_vs_API.py - сравнение агрегированных данных из 1-минутного таймфрейма и данных, полученных напрямую из API биржи

---

Чтошъ
Сравнение данных по многим метрикам показывает, что лучше брать данные напрямую с API биржи, а не агрегировать из других таймфреймов
Причем именно с той, откуда торговать возможно, т.к. алгоритмы агрегации, как оказалось, также отличаются
Цены не сильно отличаются в относительных величинах
А вот объемы местами прям да
Расхождение достигает порой сотен миллионов процентов

Как я понимаю, это связано с тем как сама биржа считает объемы, что учитывает, а что нет
Алгоритм агрегации крч

Надо еще немного подумать какие данные брать и почему
Что будет честнее перед трансформером

---

Я все-таки решил использовать данные с биржи, напрямую из API. Мой научрук сказал, что так будет правильнее, учитывается внутренняя кухня данных. Пару вопросов:
1) perp и spot объединяем в один датасет?
2) разные таймфреймы объединяем в один датасет?

---

Датасет сформирован
Проблема: проверить объемы
Вроде как, удаляется почти треть датасетов из-за объемов

