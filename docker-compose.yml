services:
  db:
    image: timescale/timescaledb:latest-pg16
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: rv_bot
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  ingestion:
    build: .
    command: python -m tg_bot.ingestion_worker
    env_file: .env
    depends_on: [db]

  predictor:
    build: .
    command: python -m tg_bot.prediction_worker
    env_file: .env
    depends_on: [db]

  notifier:
    build: .
    command: python -m tg_bot.notification_worker
    env_file: .env
    depends_on: [db]

  bot:
    build: .
    command: python -m tg_bot.bot
    env_file: .env
    depends_on: [db]

volumes:
  pgdata:
